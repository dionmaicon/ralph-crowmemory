# Ralph working files (generated during runs, at repo root only — fixtures under test/ are tracked)
/prd.json
/progress.txt

# Archive is optional to commit
# archive/

# Dry-run sandboxes
test/.sandbox/

# Ralph runtime logs (rate-limit resume)
.ralph-logs/

# OS files
.DS_Store

#Claude
.claude/
